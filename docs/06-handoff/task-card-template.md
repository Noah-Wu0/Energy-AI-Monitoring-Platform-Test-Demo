# 多模型任务卡模板

复制本模板到 `docs/06-handoff/YYYY-MM-DD-task-name.md` 后填写。

## 任务名称

填写一句话任务名。

## 背景

说明这个任务属于哪个页面、哪个模块、哪个阶段。

## 输入文件

- `path/to/input.md`
- `path/to/mock-data.ts`

## 输出文件

- `path/to/output.tsx`
- `path/to/output.md`

## 允许修改

- 允许修改的目录或文件。

## 禁止修改

- 禁止修改的目录或文件。

## 需求说明

列出要完成的具体事项。

## 验收标准

- 可以点击。
- 数据来自指定 mock 文件。
- 样式符合设计规范。
- 不破坏现有页面。

## 交接备注

记录实现假设、未完成项、后续建议。
